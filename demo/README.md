# Demo

## Option A: Local demo
1. Start stack:
   ```bash
   docker-compose -f infra/docker-compose.yml up --build
   ```
2. Upload a sample leaf image from the UI.

## Option B: Record video
- Record a 3-minute walkthrough showing upload, result screen, and crop recommendation.
