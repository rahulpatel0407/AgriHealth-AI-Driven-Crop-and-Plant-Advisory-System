# Live Scan Demo

1. Start the stack:
   ```bash
   docker-compose -f infra/docker-compose.yml up --build
   ```
2. Open http://localhost:5173 and go to Live Scan.
3. Enable consent and point the camera at a sample leaf image.
4. Watch auto-capture and open Ask for Solution.
