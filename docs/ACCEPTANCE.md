# Acceptance Checklist

- Live Scan runs with sampled frames and auto-capture behavior
- Server fallback returns candidates for frames
- Chat advisor returns answers with actions and citations
- TTS playback works for results and chat
- UI strings are in i18n JSON files
- Tests pass for live scan and chat
